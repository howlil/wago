---
interface Props {
  title: string;
  tone?: 'info' | 'warning' | 'danger';
}

const { title, tone = 'info' } = Astro.props;
---

<aside class="docs-callout" data-tone={tone}>
  <p class="docs-callout-title">{title}</p>
  <div class="docs-callout-body"><slot /></div>
</aside>
