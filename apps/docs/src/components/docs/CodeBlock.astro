---
interface Props {
  code: string;
  label?: string;
}

const { code, label } = Astro.props;
---

<div class="docs-code-block">
  {label ? <div class="docs-code-label">{label}</div> : null}
  <pre><code>{code}</code></pre>
</div>
