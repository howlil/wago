services:
  backend:
    build:
      context: ./backend
    environment:
      PORT: 3000
      HOST: 0.0.0.0
    ports:
      - "3000:3000"
    volumes:
      - ./backend/data/auth:/app/data/auth
    restart: unless-stopped
