{
  "name": "wago-simple",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.18.0",
  "type": "module",
  "scripts": {
    "check": "biome check",
    "check:fix": "biome check --write",
    "test": "pnpm --dir backend test && pnpm --dir frontend test",
    "build": "pnpm --dir backend run build && pnpm --dir frontend run build && pnpm --dir docs run build",
    "build:docs": "pnpm --dir docs run build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7"
  }
}
